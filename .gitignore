.codex
.claude
debug
.nextflow*
test/inputs/
test/runs/
run_scripts
.directory
apptainer
.Rproj.user
.history
work
token.tk
to_integrate
Docker
Out
__pycache__
.vscode
marvin_debug
adhoc_scripts
.deprecated
pyproject.toml
.mypy_cache
.pytest_cache
run_scripts
*.Rproj
subworkflows/VEP/dat/PrimateAI-3D.hg38.txt.gz

# Kate editor swap files
*.kate-swp

# FCS runtime progress log (written to work dir)
fcs_progress.log
.venv*

# Execution logs and outputs
logs/
out/

# Test directories ignored from Git
test/
tests/gui/

# Personal execution scripts (use example_*.sh templates for version control)
SBATCH_*.sh
run_phenotype_*.sh
!example_*.sh


# ── Cluster-local: personal run drivers ──
/run_cancer_*.sh
/run_carn_*.sh
/run_diet_*.sh
run_*_single_complete.sh
run_*_single_exploratory.sh
run_*_single.sh

# ── Cluster-local: sbatch wrappers & job output ──
SBATCH-scripts/
SBATCH_runner/
Slurm/
slurm-*.out
slurm-*.err

# ── Cluster-local: ad-hoc GUI test run trees ──
gui_carn_tests/
gui_diet_tests/

# ── Cluster-local: nested single-run pipeline copy ──
/single/

# ── Cluster-local: scratch work ──
/scratch/
delayed_run.log

# ── Data blobs / debug under res/ (res/*.svg, res/*.png stay tracked) ──
/res/debug/
/res/*.tsv.gz

# ── Run artifacts never to be committed again ──
*.output
*.phy.out
/ali_sp_names.txt

# ── Editor workspaces ──
*.code-workspace

# ── docs/ kept untracked (methods notes + bundled papers, curated out of band) ──
/docs/

# Keep example templates tracked (re-assert after the rules above)
!example_*.sh
